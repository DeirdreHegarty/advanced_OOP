/*
 * The following class just "encapsulates" 
 *	(or "wraps in a class") a :String declaration
 *	that could just be put into the class Dog.
 *	
 *	Have a think: what is the advantage, if any,
 *	of such "encapsulation"
 */
class Bone{
	String printString = "$==$";
}